services:
  jira:
    image: mcp/atlassian:latest
    container_name: jira-server
    ports:
      - "9000:8080"
    volumes:
      - jira_data:/var/atlassian/application-data/jira
    environment:
      - MCP_LICENSE=trial
      - MCP_PRODUCT=jira
      - MCP_PORT=9000
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G

volumes:
  jira_data: